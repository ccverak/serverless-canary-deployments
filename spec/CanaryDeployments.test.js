const { expect } = require('chai');
const { ServerlessCanaryDeployments } = require('../serverless-plugin-canary-deployments');

describe('CanaryDeployments', () => {
  describe('ApiGateway support', () => {
    it('composes the template object for the provided configuration', () => {

    });
  });
}); 

